a, b = input().split() # 표준 입력 함수 input() 호출하여 사용자로부터 입력 받은 문자열을 리턴하여 split() 메소드 호출하여 공백을 구분자로하여 분리된 문자열을 담은 리스트를 변수 a, b에 각각 할당
a = int(a) # int() 함수 호출하여 문자열 a를 정수로 변환하여 변수 a에 대입
b = int(b)

print(a) # 표준 출력 함수 print() 호출하여 변수 a 출력
print(b)

# 프로그램은 위에서부터 아래로 순서대로 실행된다.